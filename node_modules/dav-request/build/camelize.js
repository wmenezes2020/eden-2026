"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = camelize;
require("core-js/modules/es6.regexp.split.js");
/**
 * @fileoverview Camelcase something.
 */
function camelize(str) {
  let delimiter = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '_';
  let words = str.split(delimiter);
  return [words[0]].concat(words.slice(1).map(word => word.charAt(0).toUpperCase() + word.slice(1))).join('');
}